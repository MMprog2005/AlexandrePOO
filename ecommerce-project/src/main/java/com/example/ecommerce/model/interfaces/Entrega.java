package main.java.com.example.ecommerce.model.interfaces;

public interface Entrega { // Interfaces (abstração). 
    double calcularFrete(); // A Entrega do Pedido deve ser realizada de 3 formas diferentes 
}